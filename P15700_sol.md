# P15700 [2018 KAIST RUN Spring] United States Of Eurasia 题解

## 思路

每个区要求其内所有房屋的 \(x\) 坐标落在某个范围内，所以如果把所有点按 \(x\) 从小到大排序，那么每个区对应的就是一个**连续的 x 段**。

题目要求把所有房屋划分成至多 \(K\) 个这样的连续段，使得每一段内最远两点距离的最大值最小。

设这个最小值为 \(M\)，题目要求输出 \(M^2\)。

这种“最小化最大值”的形式很自然地想到：

- **二分答案** \(D=M^2\)
- 判断是否能把所有点分成不超过 \(K\) 段，使每一段的直径平方都 \(\le D\)

---

## 为什么可以按相同 \(x\) 分层

先将所有点按 \(x\) 排序。

对于相同的 \(x\)，这些点无论如何都不可能被分到不同的区。因为一个区对应某个 \(x\) 范围，而如果你在同一个 \(x\) 的中间切开，那么左右两边区间的 \(x\) 范围都会包含这个 \(x\)，从而导致同一列的点被重复覆盖，和“每个房屋恰好属于一个区”矛盾。

所以我们可以把相同 \(x\) 的点看成一层。之后问题就变成：

- 有 \(m\) 层（不同的 \(x\) 值）
- 每个区必须由若干连续层组成

代码里用 `st[i], ed[i]` 记录第 \(i\) 层在排序后数组中的起止位置。

---

## 如何判断一段是否合法

给定一段连续层 \([l,r]\)，我们需要知道这一段所有点的**直径平方**。

点集直径的标准做法是：

1. 先求这批点的**凸包**
2. 再对凸包做**旋转卡壳**求最远点对

因为最远点对一定在凸包上，所以这样做是正确的。

### 1. 求凸包

这里直接对 \([st[l], ed[r]]\) 这一整段点扫一遍，分别维护：

- 上凸壳 `up`
- 下凸壳 `dw`

再拼成完整凸包。

### 2. 旋转卡壳

设凸包点依次存到 `stk[]` 中。

对于每条边 \((stk[i], stk[i+1])\)，用双指针维护与它最远的点，更新答案即可。

这样就能得到这一段的直径平方。

---

## 如何做可行性判断

二分出某个答案 \(D\) 后，需要判断能否把所有层分成不超过 \(K\) 段，使每段直径平方都 \(\le D\)。

做法是**从左到右贪心**：

- 当前从第 \(l\) 层开始
- 找到最远的 \(r\)，满足区间 \([l,r]\) 的直径平方 \(\le D\)
- 把这一段划出去，再从 \(r+1\) 继续

为什么这样贪心是对的？

因为每次都尽量把当前段拉长，只会让后面剩下的层更少，不会比别的划法更差。所以在“段数是否 \(\le K\)”这个判定问题里，贪心最优。

### 如何快速找最远的 \(r\)

不能直接一格一格往右试，否则会很慢。

这里用了：

1. **倍增**先找到一个大致上界
2. 再在这段范围里**二分**最远合法右端点

也就是：

- 先尝试 `r+1, r+2, r+4, r+8...`
- 超过后再二分精确位置

这样单次找右端点的尝试次数更少。

---

## 整体流程

1. 按 \(x\) 排序
2. 把相同 \(x\) 归成一层，记录 `st[i], ed[i]`
3. 二分答案 \(D\)
4. 对每个 \(D\)，贪心分段检查：
   - 对区间反复调用“凸包 + 旋转卡壳”求直径
   - 看最终所需段数是否 \(\le K\)
5. 输出最小可行 \(D\)

---

## 复杂度

设不同 \(x\) 的层数为 \(m\)。

- 外层二分答案：\(O(\log V)\)，其中 \(V\) 为答案范围
- 每次判定中，要做若干次区间直径计算
- 每次直径计算需要：
  - 扫描区间内点构凸包
  - 旋转卡壳求直径

所以这题本质上是一个：

- **二分答案 + 贪心分段 + 凸包直径判定**

实现量比较大，但思路是很标准的几何判定流程。

---

## 做法小结

这题关键有三步：

1. 认识到区必须对应一段连续的 \(x\) 层，所以先按 \(x\) 分层。
2. 二分最大允许直径平方。
3. 判定时贪心分段，而每段是否合法用**凸包 + 旋转卡壳**求直径。

因此整题就是一个经典组合：

**二分答案 + 贪心 + 计算几何**。