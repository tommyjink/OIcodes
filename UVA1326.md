### 题目描述
西伯利亚的古生物学家最近发现了一些侏罗纪时期恐龙骨骼的碎片。古生物学家决定将这些碎片运往古生物博物馆。不幸的是，这只恐龙体型巨大，没有能装下这些碎片的箱子。因此，他们决定把恐龙骨骼碎片拆分成单独的骨头，运往博物馆后再进行重新组装。为了便于重新组装，骨骼相互分离的关节处都贴上了特殊标签。与此同时，在包装好碎片后，又发现了一些新的骨头，于是决定将它们与主要的碎片一起寄出。所以这些新骨头被放入包裹并寄往了博物馆。

然而，当包裹到达博物馆时出现了一些问题。首先，并非所有标记关节的标签都是不同的。也就是说，使用了从'A'到'Z'的字母作为标签，每两个需要连接的关节都标有相同的字母，但可能有多对关节标有相同的字母。

此外，同样类型的标签也用于给包裹中新增的骨头做标记。因此，可能存在标有关节标签但无需与其他骨头相连的骨头。不过，每块骨头最多只有一个关节标有某个特定字母，这在一定程度上减轻了问题的复杂性。

你的任务是帮助博物馆工作人员恢复一些可能的恐龙骨骼碎片。也就是说，你必须找到这样一组骨头，它们可以相互连接，并且满足以下条件：
- 如果某个关节与另一个关节相连，那么它们标有相同的标签。
- 对于这组骨头中的每一块，标有某个标签的关节都与其他关节相连。
- 使用的骨头数量尽可能多。

注意，两块骨头可以通过多个关节相连。
### 输入格式
输入包含多个数据集。每个数据集的第一行包含一个整数N，表示骨头的数量（\(1 \leq N \leq 24\)）。接下来的N行包含骨头的描述：每行包含一个由不同大写字母组成的非空序列，这些字母代表标记相应骨头关节的标签。
### 输出格式
对于每个数据集，在输出文件的第一行打印L，即重新组装骨骼碎片时可能使用的最大骨头数量。然后，在另一行按升序输出L个整数，表示要使用的骨头编号。骨头按输入文件中的顺序从1开始编号。
### 样例输入
```
1
ABC

6
ABD
EG
GE
ABE
AC
BCD
```
### 样例输出
```
0

5
1 2 3 5 6
```