# D 暴力

直接暴力枚举整个二进制串。

设当前位置为 `x`，递归枚举这一位填 `a` 或 `b`。每当某个要求回文的前缀长度正好被填满时，就直接暴力检查这个前缀是不是回文串；如果不是，立即剪枝。

最后统计所有合法字符串个数，对 `m` 取模输出。

时间复杂度大约是 `O(2^N\cdot N)`，只适合 `N` 很小的情况。

## 文件

- [DD.cpp](/Users/chenghanjin/Documents/CPP/2026-3-20/DD.cpp)
